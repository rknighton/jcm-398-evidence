The Arc 4 follow-up evidence has been consolidated into a separate review package:

<NEW_ISSUE_URL>

The key judgment is that small NumPy versus pure-Python scoring differences were already expected and can change rankings, but the completed semi-realistic and fixed real-codebase comparisons did not demonstrate a practically significant problem. The evidence does not prove perfect safety or establish a production failure rate. On balance, it supports continuing with the faster method, preferably with exact checking at uncertain result boundaries.

The new issue keeps the full evidence discussion separate from #398 while linking back to the original context here.
