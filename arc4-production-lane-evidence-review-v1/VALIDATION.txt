Arc 4 production-lane evidence review v1
Third-party review gate: 2026-08-08 America/New_York

Release-quality checks:
- Package verifier: PASS, 876 manifest-covered files of 877 total, about 1.96 GiB. The
  877th file is CHECKSUMS.sha256 itself, which cannot list its own digest. A previous
  revision of this receipt said 875 of 876; that was written before
  evidence/full-suite-replay/harness/mechanism_census.py and
  evidence/full-suite-replay/mechanism-census.json were added, and was left stale. The
  verifier always printed the correct live count, so this was receipt drift and never a
  manifest defect.
- Package verifier unit tests: PASS, 19 tests. They exercise the verifier's machinery on
  synthetic packages and deliberately do not run the headline recomputation against real
  data; running verify_package.py is what does that.
- Recomputed by the verifier, not read from a summary: the 5,000 query records; the 33
  screen nominations; the 5 complete-replay findings with positions 38, 65, 65, 76, 78 and
  their rank-0, top-25 and top-100 membership outcomes; 3,211 geometric rank-0 changes,
  derived by pairing the two shipped geometric lane outputs; 10,002 geometric cases; the
  55 attempt, 96 comparison-v1 raw and 360 full-ranking file counts, and the 36 distinct
  contents behind those 360; the full-suite replay's rank 0, ordered top-k, membership and
  exact-tie results, derived from its per-lane records; lossless reconstruction of the one
  gzip-stored dataset; every file against its manifest digest; and the local-path
  disclosure in both directions.
- Cross-checked, not recomputed: every numeric claim in INDEX.json against the surface
  that owns it; the comparison-v1 and comparison-v2 headline counts against their
  experiment summaries; every CLAIM-LEDGER.csv evidence pointer against the filesystem.
- Not covered by the verifier: comparison-v2 m5, m6, m10, m11 and m12. An external review
  run on 2026-08-07 did independently reproduce m1 through m6, m10 and m12 from
  packet/raw/full-rankings/ under the positive-score ranking rule in harness/metrics.py,
  matching SUMMARY.json exactly, including m5_full 820, m6_full 540, 80 tie-differing
  pairs over 8 problems, and the m12 histogram with none=20. m11 was not reproduced.
- Supplemental replay reproducer: evidence/full-suite-replay/harness/compare_lanes.py PASS,
  standard library only, no corpus database and no install required. Read-only by default:
  it re-derives the replay's reported numbers, compares them byte for byte against the
  committed artifacts, fails on drift, and reproduces all five previously published
  findings with byte-identical top-100 lists in both lanes. Drift detection was exercised
  by mutating results-summary.json, which produced exit 1 and a hash-mismatch message.
  It is a reproducer, not the release gate; verify_package.py is the release gate.
- Existing repository root verifier: PASS, 26 of 26 checks reproduce.
- Existing guarded-candidate verifier self-test: PASS, 360 rows and 97 claims.
- The four retained experiment verifiers do NOT run in this layout, by design. Their
  observed exit codes and causes are recorded in RETAINED-VERIFIERS.md. This is a change
  from the previous receipt, which named only one of them.
- JSON parse: PASS, 184 files. Python syntax parse: PASS, 57 files. Both counts cover
  every matching file in the package with no exclusions, and both were 183 and 56 in the
  previous revision for the same stale reason as the file count above.
- Claim ledger parse: PASS, 13 rows, 13 unique claim IDs, every path pointer resolves.
- Replay-to-source reconciliation: PASS. Both lanes join to the 5,000 shipped query records
  on identity, corpus and vector hash with no missing, extra, duplicate or mismatched row,
  checked in both directions, with two negative tests.
- Attested but not gated: evidence/full-suite-replay/mechanism-census.json is derived from
  the three excluded corpus databases. verify_package.py does not check it, and no
  decision-facing claim in REPORT.md, CLAIM-LEDGER.csv or the issue comment rests on it.
  That last sentence was not true in the previous revision even though it was written
  there. REPORT.md made "investigate canonical ordering" recommendation 2, CLAIM-LEDGER
  C12 made the top-level engineering judgment conditional on canonical ordering, C09
  ranked boundary rescoring "secondary to canonical ordering", and the issue comment gave
  the hypothesis an advocacy paragraph. All four were withdrawn in this revision. The
  census, its derivation script, its claim-ledger row and its attested_not_gated label are
  all retained unchanged; only the decision weight was removed.
- Local research paths: 79 files retain them, 881 occurrences, every one declared in
  LOCAL-PATH-DISCLOSURE.json and confirmed by the verifier in both directions. The
  previous receipt reported no matches; that was wrong, because the check searched for a
  single-backslash form and the records use the JSON-escaped form. See
  PROVENANCE-ANNOTATION.md.
- High-confidence credential-pattern scan: PASS, no matches. A byte-reading scanner also
  reports only the declared research paths, the deliberate arc4@example.invalid
  placeholder, loopback 127.0.0.1 in test fixtures, and three byte coincidences inside the
  gzip stream.
- Internal task identifier and superseded artifact scan: PASS, no matches, over every file
  rather than six extensions.
- Forbidden Unicode punctuation scan: PASS over every file whose bytes decode as UTF-8.
- Repository-file limit: PASS. Largest file 45.42 MiB, below the 95 MiB gate.
- Multi-file archive check: PASS, no ZIP files.
- Git ignore check: PASS, no package files are ignored.
- gitleaks: not installed; the named executable was checked and not found.

Unpacked dataset state:
- All 5,000 provider-text queries are present, and all 5,000 have now been replayed
  through both shipped lanes; see evidence/full-suite-replay/.
- All geometric inputs supporting 10,002 executed cases are present, together with both
  lane outputs from which the 3,211 rank-0 changes are recomputed.
- All 55 adversarial attempt files are present, including failed and superseded attempts.
- All 96 comparison-v1 raw outputs are present with their controls.
- All comparison-v2 raw rows, warmups, 360 full-ranking files, controls, and invocation
  artifacts are present. Those 360 files hold 36 distinct contents, because each of the 10
  repetitions of a problem produced byte-identical output.
- The 188.48 MiB paired JSONL is stored as one deterministic 19.80 MiB gzip file whose
  decompression reproduces 197,630,471 bytes, 132 lines, and SHA-256
  bfa58e4dabb95f9a765c1c3514eede5fdbb6d80adf263f8eae3eec19161685ef.

Version and publication boundary:
- The measured software target is pinned to jcodemunch-mcp v1.108.228, as requested in
  issue 398 comment 5177953577. The package makes no rolling claim about the latest
  release or current main. Later releases do not alter what this experiment measured.
- Compatibility with a later implementation is a separate question only if a relevant
  ranking, membership, truncation, or scoring behavior changes. Routine release cadence
  does not require this historical packet to be revalidated.
- This evidence package is published on a public review branch. The upstream issue 398
  follow-up comment has not been posted. No pull request, merge, release, or tag was
  created for this publication update.
